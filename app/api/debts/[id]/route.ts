import { NextResponse } from "next/server";
import { query } from "@/lib/db";
type Context={params:Promise<{id:string}>};
export async function PATCH(request:Request,context:Context){try{const{id}=await context.params;const body=await request.json();if(!body.name?.trim()||!["owed_by_me","owed_to_me"].includes(body.direction)||!Number.isInteger(body.principalAmount)||body.principalAmount<=0)return NextResponse.json({error:"Lengkapi nama, arah, dan nominal positif."},{status:400});const result=await query("UPDATE debts SET name=$1,direction=$2,principal_amount=$3,due_date=$4,description=$5,updated_at=NOW() WHERE id=$6",[body.name.trim(),body.direction,body.principalAmount,body.dueDate||null,body.description?.trim()||null,id]);if(!result.rowCount)return NextResponse.json({error:"Utang tidak ditemukan."},{status:404});return NextResponse.json({ok:true});}catch{return NextResponse.json({error:"Utang tidak dapat diperbarui."},{status:500});}}
export async function DELETE(_request:Request,context:Context){try{const{id}=await context.params;const total=Number((await query<{total:string}>("SELECT COUNT(*) total FROM debt_payments WHERE debt_id=$1",[id])).rows[0].total);if(total>0)return NextResponse.json({error:"Hapus semua pembayaran terlebih dahulu."},{status:400});if(!(await query("DELETE FROM debts WHERE id=$1",[id])).rowCount)return NextResponse.json({error:"Utang tidak ditemukan."},{status:404});return NextResponse.json({ok:true});}catch{return NextResponse.json({error:"Utang tidak dapat dihapus."},{status:500});}}
